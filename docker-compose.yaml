version: "3"
services:
#  crawler:
#    build:
#      context: ./src/main/docker
#    volumes:
#     - .:/tmp
  db:
    container_name: db
    image: postgres:latest
    ports:
    - "5432:5432"
    volumes:
      - ./postgres-data:/var/lib/postgresql/data
    # links ? to connect crawler and db
    # https://docs.docker.com/compose/compose-file/#links

